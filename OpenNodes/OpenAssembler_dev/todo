++ shortcut for run
-+ more attribute editor type (boolean, file, functioncall,uiItem)
-- to show script and reference links as a connection line, with different color
++ reload the node and menu database
-- in attribute editor, on the led show from to of a connection
-- integrate text type, with a special save box
-- integrate the help description for the nodes
-- embadding node network into a node
-- outliner
-- integrate text editor into oas
-- move the screen with the middle button
-- multy-duplicate nodes
-- backdrop items for the editor window
-- colorise the nodes depending on the caregory
-- node z values need to be solved
-- better error visualisation (errors are red, and so on...)
-- keyboard shortcust for open, close, save...
-- show the scene name in the title, indicate if it needs to save
-- when closeing, ask for save