### connect <from\_node.output> <to\_node.input> ###
This will connect two node.

Example:
```
connect mult_01.out printer_01.A
```

Shortversion:
  * **cn** it will work on the same way like the long command